#include<stdio.h>

void main()
{
/*	int Divident,Divisor,Remainder,Quotient;
	printf("Enter DIVIDENT and DIVISOR accordingly\n");
	scanf("%d%d",&Divident,&Divisor);
	Quotient=Divident / Divisor;
        Remainder=Divident % Divisor;
	printf("QUOTIENT FOR %d and %d is %d\n",Divident,Divisor,Quotient);
        printf("Remainder FOR %d and %d is %d\n",Divident,Divisor,Remainder);
*/
	char c;
	for(c=1;c<256;c++)
	{
		printf("%c\t",c);
	}



}
